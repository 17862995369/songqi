
   <swiper indicator-dots="{{indicatorDots}}"
  autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}" circular="{{circular}}">
  <block wx:for="{{banner}}">
    <swiper-item>
      <image src="{{item}}" class="slide-image" mode='widthFix'  style="height:100%;width:100%;"/>
    </swiper-item>
  </block>
</swiper>